# 2026-09-09

- 孔明灯（kongming-deng）：将底部「祝福词」大背景 `#ui` 由渐变暗色改为毛玻璃（`var(--glass)` + `backdrop-filter:blur(18px) saturate(140%)`），内层 `.wish-input` 背景调淡至 0.16。本地预览 `http://127.0.0.1:8731/`（python http.server 后台运行中）。
- 风铃物语（wind-chime）：生成竖屏 3:4 游戏风封面。先基于项目现有视觉资产（夜蓝底 `#0a0c22`、雾蓝紫 `#cdd6ff`、暖金 `#ffe2bb`）给出构图方案并确认方向；用 ImageGen 生成场景图（仲夏夜庭、风铃、月与萤火），再用 Pillow + STHeiti 叠加标题/副文案/角标，盖住 AI 水印。成品 `wind-chime/covers/风铃物语_封面.png`（1072×1440，近似 3:4）。
  - 用户反馈：AI 凭空画的氛围图与游戏实际 three.js 渲染「风格/造型」不符。真实风铃是「帘式」：横杆下 12 串软绳（`N_STRAND=12`）、每串 10 节（`PER=10`）淡彩管珠（`STRAND_COLOR` 粉彩浅色），并非 3 大铃+木盘+6 金属管。改方案：直接无头 Chrome 截游戏真实渲染当封面底图，再叠加标题。
  - 封面落地：用 agent-browser 自带 Chromium 无头跑 index.html → click `#startBtn` + 隐藏 `.hud-title`/`.hud-bottom` → 截图 `covers/scene_raw.png`（1280×577，真实帘式 12 串风铃）；再 PIL 合成 `covers/风铃物语_封面.png`（1080×1440，3:4）。合成：背景接游戏同款夜蓝渐变、风铃下移、标题/副文案/角标叠加。用后已 `rm -rf ~/.agent-browser/browsers` 删掉 Chromium 缓存。
